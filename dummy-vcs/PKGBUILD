pkgname=dummy-vcs
pkgver=1
pkgrel=1
pkgdesc="A dummy package to replace the package you don't need"
arch=('any')
_replaces=(breezy subversion mercurial)
provides=(${_replaces[*]})
conflicts=(${_replaces[*]})
